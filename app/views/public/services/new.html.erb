<h1>体験新規登録</h1>
<%= form_with(model: @service, url: services_path, method: :post) do %>
  <div class="flex-container">
    <%= f.label :image, "画像" %>
    <div class="image_field">
      <%= attachment_image_tag :image, format: "jpeg", id: "img_prev", fallback: "no-image.png", class: "item_image" %>
      <span class="btn btn-file btn-info"><%= f.attachment_field :image %>画像選択</span>
      <p>選択するとプレビューされます。<br>画像未選択のままだと、<br>「画像準備中」になります。</p>
    </div>
    <table class="admin_item_info item_form">
      <thead>
        <tr>
          <th></th>	
          <th></th>
          <th></th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= f.label :catchphrase, "キャッチフレーズ" %></td>
          <td><%= f.text_field :catchphrase %></td>
        </tr>
        <tr>
          <td><%= f.label :place, "場所" %></td>
          <td><%= f.text_area :place, rows: 3 %></td>
        </tr>
        <tr>
          <td><%= f.label :content, "説明" %></td>
          <td class="select_field"><%= f.collection_select :genre_id, Genre.all, :id, :name %></td>
        </tr>
        <tr>
          <td><%= f.label :point, "必要ポイント" %></td>
          <td><%= f.number_field :point %> 円</td>
        </tr>
        <tr>
        </tr>
      </tbody>
    </table>
    <div class="button">
      <%= f.submit "登録", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>