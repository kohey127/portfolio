<div class="history">
  <div class="exp_histories">
  <h2 class="icon">EXP獲得履歴</h2>
    <table>
      <thead>
        <tr>
          <th>日時</th>
          <th>相手</th>
          <th>ポイント残高</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= current_customer.created_at.strftime("%Y年%m月%d日 %H:%M") %></td>
          <td>管理者</td>
          <td>0</td>
        </tr>
        <% @exp_histories.each do |point| %>
          <tr>
            <td><%= point.created_at.strftime("%Y年%m月%d日 %H:%M") %></td>
            <td><%= point.trigger.name %></td>
            <td><%= point.balance %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <div class="point_histories">
  <h2 class="icon">ポイント履歴</h2>
    <table>
      <thead>
        <tr>
          <th>日時</th>
          <th>相手</th>
          <th>ポイント残高</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= current_customer.created_at.strftime("%Y年%m月%d日 %H:%M") %></td>
          <td>管理者</td>
          <td>500</td>
        </tr>
        <% @point_histories.each do |point| %>
          <tr>
            <td><%= point.created_at.strftime("%Y年%m月%d日 %H:%M") %></td>
            <td>
              <% if point.trigger.id == 1 %>
                管理者
              <% else %>
                <%= point.trigger.name %>
              <% end %>
            </td>
            <td><%= point.balance %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>